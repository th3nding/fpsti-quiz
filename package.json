{
  "name": "fpsti-quiz",
  "version": "1.0.0",
  "description": "FPS Personality Type Indicator Quiz - Pure static HTML site",
  "private": true,
  "scripts": {
    "build": "echo 'Static site, no build required' && exit 0",
    "start": "echo 'Static HTML site - open index.html in browser' && exit 0",
    "deploy": "echo 'Deployment handled by Cloudflare Pages, no wrangler needed' && exit 0",
    "preview": "echo 'Preview not available for static site' && exit 0"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": ["fps", "quiz", "personality", "static", "html"],
  "author": "",
  "license": "MIT"
}